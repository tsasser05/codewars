package kata

const (
  w = "white"
  b = "black"
)

func BlackOrWhiteKey(key int) string {
    keys := []string {w, b, w, w, b, w, b, w, w, b, w, b}
    return keys[((key  - 1) % 88) % len(keys)]
}


